# Projects

This directory holds separate project work areas for report generation and review.

Each project should keep project-specific inputs, generated context, source status, notes, generated previews, and future report materials separate from shared application code and documentation.

## Current Projects

- Create or select project workspaces under `projects/<project_id>/`. Generated project outputs are local workspace state and are not committed by default.
